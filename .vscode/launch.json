{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run/Debug active C file (host)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build-host/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "Build active C file (host)"
        }
    ]
}
